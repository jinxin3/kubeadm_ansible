apiVersion: kubeadm.k8s.io/v1beta1
kind: ClusterConfiguration
kubernetesVersion: {{kubernetes_version}}
apiServer:
  certSANs:
  - "{{haproxy_hostname}}"
controlPlaneEndpoint: "{{haproxy_hostname}}:6443"
networking:
  podSubnet: {{pod_network_cidr}}
